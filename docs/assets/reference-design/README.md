# Reference Design Screenshots

This folder stores the original UI screenshots used as the visual reference for `docs/master-plan.md`.

Save the planning screenshots with these exact filenames:

- `screenshot-01-project-detail-task-list.png`
- `screenshot-02-project-list-overview.png`

`docs/master-plan.md` already embeds those paths in `MP-05. UX And Visual Direction`. Once the image files are placed here, Markdown viewers will render them directly in the master plan.

## Screenshot 01: Project Detail Task List

Defines:

- dark sidebar and app shell;
- back button and project header;
- progress/deadline cards;
- tabs;
- add-task control;
- task filters;
- task rows with completion, labels, deadlines, and checkboxes.

## Screenshot 02: Project List Overview

Defines:

- first authenticated screen;
- sidebar active state;
- search, category filter, sorting control;
- primary create button;
- project rows with icons, progress bars, deadlines, last visit, and row menu.

